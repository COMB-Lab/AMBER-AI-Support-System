Notebooks can be ran  if you include the path to the amber image file in the top code block.

Without the code block it will not run and throw errors.

Make sure apptainer is installed as it will not run aswell. 
